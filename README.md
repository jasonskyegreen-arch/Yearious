# Yearious

The app lives in [yearious/](/Users/jason/OneDrive/Desktop/coding/Yearious-main/yearious).

Run it from the repo root with:

```powershell
npm.cmd run dev
```

Or run it directly inside `yearious/` with:

```powershell
npm.cmd run dev
```
